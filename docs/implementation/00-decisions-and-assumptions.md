# Decision Log & Assumptions

Every decision that closes an ambiguity in the PRD or the safety review is recorded here with
an ID. Requirements, design, tasks, and tests cite these IDs. If a decision is reversed, the
citing artifacts are the change list.

**Source artifacts:** [`../interviewpreprecallprd.md`](../interviewpreprecallprd.md) (product plan),
[`../build-plan-safety-review.md`](../build-plan-safety-review.md) (pre-implementation review, amendments A1–A23).

---

## Decisions requiring the user — resolved

| ID | Question | Decision | Consequence |
|---|---|---|---|
| **D-U1** | v1 scope | **In:** multiple note sets (US-A3), cloud STT (FR18–21), progress tracker (Epic G). **Out of v1:** `.docx` import (FR1b), deferred to post-v1 (M9 is the last v1 milestone, so "M9" was an ambiguous label for the deferred bucket). | Epic G moves from stretch to core. Data model is multi-set from day one. |
| **D-U2** | Both audio streams | **Both interviewer (loopback) and user (mic) capture are mandatory in v1.** FR6's "optionally capture microphone" is superseded. | Mic is no longer an Epic G dependency that can be dropped; it is a session prerequisite. Preflight must validate both devices. |
| **D-U3** | LLM failure behavior | **Show the stage-1 fallback match, visually marked as degraded**, gated behind a higher confidence bar (τ_degraded). | Resolves the §10b vs US-D2 contradiction. Overlay has two visual states for content: confirmed and degraded. |
| **D-U4** | Target platform | **Windows 11.** | FR14 capture exclusion is available. The startup check (FR38) is still built, but as a guard, not a gate on an uncertain platform. |
| **D-U5** | Panic clear lifecycle | **Wipe, stop capture, stay resumable.** Purge clears buffers, transcript, and overlay; capture stops; the session holds in a `WIPED` state that resumes in ~1 s without re-running preflight. | Adds a seventh state to §6. Resolves the contradiction between a state machine that ended the session and an FR60 single-press control implying it was cheap. The user gets an unambiguous stop *and* a fast recovery — the two properties that were in tension. |
| **D-U7** | Overlay palette | **The overlay is a neutral translucent gray panel, not PRISM's plum** — stated by the user as overriding PRISM. PRISM still governs the overlay's typography, 20px radius, spacing ladder and semantic rail colours, and governs the app chrome entirely. | A saturated surface tints the video behind it; a neutral translucent one does not. Scoped to the overlay so the rest of the product stays on-brand. **D-U7a resolved: neither — brightness is a user control** (FR65), spanning a dark band and a light band with the unreadable mid-gray stepped over. Opacity stays a separate control (FR24). |
| **D-U6** | Target hardware | **Two machines: a 2025-or-newer laptop, and a desktop (AMD Ryzen, 32 GB RAM, NVIDIA RTX 5070).** | See the measurement discipline below — this one has a trap in it. |

### D-U6 measurement discipline — read before running the AS-1 gate

The desktop has a discrete GPU. `faster-whisper` will use CUDA if it finds it, and would produce an
inference-tail number several times better than any laptop achieves. **Measuring AS-1 there, with
CUDA active, validates a machine most sessions will not run on** and would green-light a local
default that misses the target in practice.

So:

1. **The AS-1 gate is measured CPU-only, on the 2025 laptop.** `device="cpu"`, CUDA explicitly
   disabled. This is the number that decides whether local STT can be the default (FR18), because
   NFR2 requires the product to work without a discrete GPU.
2. **The desktop's CUDA number is recorded separately** as an upper bound, and never substituted
   for (1).
3. **CUDA is used opportunistically at runtime** (NFR7): if a compatible GPU is present, the local
   backend uses it and the user gets a faster session for free. It is an optimisation, never a
   requirement.

If the interviews themselves will be taken on the desktop, the practical latency will be the CUDA
number — but the gate stays CPU-anchored, because a build that only works on a 5070 is not the
product described in the PRD.

## Decisions made by engineering judgement — recorded, reversible

| ID | Decision | Rationale | Reverse cost |
|---|---|---|---|
| **D-1** | **Concurrency backbone is threads + bounded queues + Qt signals.** `asyncio` is confined inside the cloud STT backend, which owns a private event loop in its own thread. No application-wide event loop. | The default path (local STT) needs no async. Making the whole app async to serve an optional backend inverts the cost. `faster-whisper`/CTranslate2 and torch release the GIL during inference, so threads genuinely parallelize the CPU-bound work. Qt's queued signal/slot connections are the supported way to cross into the GUI thread. | High after M3. This is the load-bearing decision — pick it now or fork the codebase later. |
| **D-2** | **The STT interface is specified before any backend is written** (§2 of the design doc), and is written against the *local* backend's constraints first. | Review A8/BC-4. PRD Phase 2 builds cloud first, which would shape the interface around WebSocket conveniences (native interim results, server-side finalization) that local Whisper cannot provide. | Low now, high later. |
| **D-3** | **Notes persist as one JSON file per note set** under `%APPDATA%`, atomic-write + 5-deep backup rotation. Embeddings cache to a separate `.npz` keyed by note-set UUID + embedding-model slug, with per-note content hashes stored inside it for selective re-embedding (design §4). | Human-readable, diffable, trivially exportable (FR30), no DB dependency. The corpus is a few thousand words — no query engine is warranted. | Low. Format is versioned (FR31). |
| **D-4** | **"Utterance" is defined as a finalized transcript span**, terminated by ≥700 ms silence, 10 s max duration, or session stop; minimum 3 words and 12 characters. Matching runs per utterance, never per audio chunk. | PRD FR8 (2–4 s audio chunks) and FR9 ("each transcribed utterance") are different units. Matching per audio chunk would fire mid-sentence and multiply LLM calls ~3×. | Low. Tunable constants. |
| **D-5** | **Overlay content is user-authored verbatim text only.** Notes carry an explicit `bullets[]` field, authored or confirmed by the user at import. Nothing is summarized at match time. | Resolves FR11 ("headline + 1–3 bullets") against §4 retrieval-only. Structure is chosen by the user at import, which FR2 already requires to be reviewable, so no generation is needed at match time. | Low. |
| **D-6** | **A note with no bullets renders as headline + verbatim body truncated at a sentence boundary (≤240 chars)**, and is flagged "not overlay-optimised" in the editor. | Degrades honestly rather than blocking, and still emits zero generated text. | Low. |
| **D-7** | **Session lifecycle is an explicit state machine**; health is an orthogonal attribute, not a state. | Prevents the combinatorial `RUNNING_BUT_STT_DEGRADED_AND_CLOUD_FALLEN_BACK` state explosion that ad-hoc booleans produce. | Medium. |
| **D-8** | **Speaker attribution is stream-based with echo detection**, not diarization. Preflight cross-correlates mic and loopback; high correlation warns the user that headphones are needed. At runtime the **echoed mic span is dropped from the tracker**; the interviewer utterance is never suppressed (design §5a). | RC-8. D-U2/D-U1 promote this from a stretch caveat to a v1 correctness concern. Full acoustic echo cancellation is out of scope for v1. | Medium. |
| **D-9** | **The Anthropic model ID is configuration, not a constant** (default `claude-haiku-4-5-20251001`). | BC-5. Pinning is right; hard-coding it into a PyInstaller exe means a deprecation requires a rebuild. | Low. |
| **D-10** | **Matching runs only on the interviewer stream.** The mic stream feeds the progress tracker exclusively. | The product surfaces prep in response to what the interviewer asks (D-U2's stated intent). Matching on the user's own speech would surface notes about what they just said. | Low. |
| **D-11** | **One stage-2 LLM call in flight at a time, plus one pending slot; latest-issued wins.** A superseded in-flight request is **not cancelled** — Python cannot cancel a blocking call on a pool thread — it runs to its 5 s timeout and its response is discarded by the sequence gate. See design §5a for the dispatch policy. *(Corrected: this previously said superseded requests "are cancelled", which the design explicitly says is impossible and must never be relied on.)* | FR32 as amended. Also bounds cost and rate-limit exposure (RC-5). | Low. |
| **D-20** | **The STT worker restart budget resets when preflight passes.** | FR61 is per session; the counter previously persisted for the process lifetime. | Low. |
| **D-21** | **`BoundedFrameQueue.push` always copies into a `bytearray`.** | `zero()` cannot wipe immutable `bytes` (FR15 would be silently false), and WASAPI callbacks reuse a scratch buffer, so storing the caller's array by reference would corrupt queued frames. | Low. |
| **D-22** | **A panic clear is undone only by the user** — `resume(automatic=True)` from `WIPED` raises. | A device-return callback firing after the button was pressed would restart capture the user deliberately stopped. | Low. |
| **D-23** | **The LLM degradation switch is applied to the pipeline before the indicator changes**, and toggling it with no pipeline attached raises. | An indicator claiming local-only while the API is still being called is a privacy-facing false statement, not a cosmetic lag. | Low. |
| **D-24** | **Purge runs every step even when a hook throws**, collecting failures rather than aborting. | `cancel_network` runs first and is the most likely to fail; aborting there leaves capture running and nothing cleared, on the degraded session that most needs the purge. | Low. |
| **D-25** | **The capture clock is re-anchored on every (re)connect.** A reconnect starts a new server-side stream numbered from zero. | Without it the first post-reconnect event maps tens of seconds backwards, the ordering guard discards it and every event after it, and the stream goes permanently silent while reporting READY. | Low. |
| **D-26** | **`x = y or Default()` is banned for injected collaborators; use `if y is None`.** | `DiagnosticRing.__len__` makes an empty ring falsy, so the idiom silently discarded an injected ring and wrote to an orphan. It was present in already-merged code (`MatchingPipeline`, `SessionManager`, `Preflight`). | Low, but it was live. |
| **D-27** | **Cloud egress is lit before the socket opens and cleared only after it closes.** | The error is then always in the over-reporting direction. An indicator saying "not leaving the device" while a socket is open is a false privacy statement; saying it a handshake early is cosmetic. | Low. |
| **D-51** | **§9b's two-line bullet allowance is a floor, not a cap.** The allowance is measured from the space the panel actually has, and never drops below two. | Read as a flat cap it clips text at every size above the minimum — a 600px panel would show two lines with most of its height empty, which is the clipping FR23 forbids. The font is a function of height alone (§9b rule 2), so "after scaling has hit the floor" is not an event elision can wait for. | Low. At the minimum size the measured value *is* two. |
| **D-52** | **`MainWindow` requires an injected settings store; it does not build a `QSettings`.** | A widget constructor that mints a registry handle reaches the user's real settings from every test that builds one, and the T0.4 allowlist guard cannot see it — `QSettings` writes through Qt's C++ layer, not Python's `open`. Injection is what every other collaborator here gets. | Low. One production call site passes `default_settings()`. |
| **D-53** | **Widgets this window opens are parented to it, and callbacks close over data rather than over `self`.** | Two segfaults, not style. A parentless top-level widget destroyed through a Python reference, and a window→panel→bound-method→window cycle collected by the GC, both let Qt destroy objects in an order nobody chose. The second one killed the interpreter only after ~20 windows, so it presented as an unrelated test crashing. | Low, and it removes coupling the panel never needed. |
| **D-54** | **The `qapp` fixture tears down: top-level widgets are closed and deleted while the application is still alive.** | Six modules each defined their own copy and none tore anything down, so every widget survived to interpreter shutdown. That is an intermittent exit-time segfault — the suite reports all tests passing and the process dies with 139, which on CI is a red build with a green test report. | Low. |
| **D-55** | **FR72's per-kind marking is a shape, not a colour** — a glyph per `SourceKind`, prefixed to the headline at display time, plus the kind's name as the reading channel. §9b gains the token table it was missing. | Every colour on the overlay already means a *state*: FR51's rail, FR20's amber egress dot, FR14a's red bar, FR12's green mark. Five kind hues would collide with one of those or remap a PRISM semantic dot, which PRISM §1 forbids outright. §9b already requires colour never to be the only channel; kind takes that other channel outright and inherits ink already measured against both brightness bands, so it adds no contrast surface. | Low here; **the 1 m glance test and the bundled-font glyph coverage are unverified** — real-surface checks, T5.9 and T9.4. |
| **D-56** | **A report exports as plain Markdown to a path the user picks, and the export says — in the file — that it is an unencrypted copy.** | FR82 encrypts every *stored* copy to the Windows account; an export is a deliberate hole in that, because a report the user cannot get out of the app is worth little. Markdown rather than a new format follows FR30's notes bundle. The warning travels in the file because the dialog that wrote it is long gone by the time the file is mailed to someone, and FR82's promise would otherwise read as broader than it is. | Low, and it is the one place the encryption boundary is crossed on purpose. |
| **D-57** | **The report reader renders generated text with `setPlainText`, never rich text.** | `QTextEdit` interprets HTML. This is the only string in the product that came from a language model, and a model that emitted markup would have it rendered — including anything that looks like a link. One call is the whole mitigation, so it is asserted in a test rather than left as a habit. | Low. |
| **D-58** | **A session stores a snapshot of the context set it was held against, and a report is graded against that snapshot.** A session with no readable snapshot falls back to today's set and **the report says so, on the page**. | The transcript already travelled with the tracker's coverage verdict for exactly this reason; the rest of the context did not, so a report regenerated a week later graded its JD-fit and resume sections against whatever notes happened to be loaded — silently, in a document the user reads about themselves. Storing the set id instead would be cheap and useless: the set is mutable, so the id resolves to something that is no longer what the interview was held against. The snapshot rides **inside the existing encrypted envelope** (FR82) and is deleted with the session (FR84), inheriting both guarantees; D-3 puts the corpus at a few thousand words, so the size objection does not survive contact with the numbers. | Low. Old sessions keep working and are marked, not refused. |
| **D-59** | **Report generation is split into `prepare` (refusals + prompt, GUI thread) and `send` (the network, a worker).** The view dispatches only `send`, on a dedicated daemon thread, and marshals the result back through a Qt signal. | The model call is seconds long and FR81's confirmation has to be asked where dialogs live, so a single blocking method forced the upload onto the drawing thread. **FR81a was the casualty**: the egress indicator was lit in memory and dark on screen for exactly the seconds it exists to announce. Splitting at the payload rather than at the confirmation is what keeps FR81 honest — the size shown and the size sent come from one `PreparedReport`. Not the application's executor: that pool serves matching on a latency budget (D-11), and a multi-second upload parked in it would sit in front of a stage-2 call. | Low. `generate()` still does both halves for scripts and tests. |
| **D-60** | **A collaborator hook whose absence breaks a feature does not get a no-op default.** Either it is a required argument, or the object that owns the wire clears it back to a *named* no-op that says what it is. Every `on_*` hook is swept for a production assignment when its feature is built. | `Application.on_result = lambda _result: None` hid T5.10 for six milestones: matching ran, the panel existed, and nothing rendered. Three more of the same shape landed in a fortnight — the retention sweep with no caller, `HealthMonitor` with no observer, `start_clock` never called. In every case both halves had passing tests and the *join* was untested and unowned, because a no-op default makes an unwired hook indistinguishable from a wired one at runtime and at type-check time. | Low. The sweep is a `grep`; the cost is remembering to run it. |
| **D-61** | **The active note set cannot be switched while a session is running** (FR43), and switching goes through `Application.activate_context_set`, which rebuilds the embedding index and re-points the prefilter. | Two failures in one decision. The index is *built* from the set and `Prefilter` keeps its own reference, so assigning `context_set` would leave matching drawing from the previous corpus — silent, and indistinguishable from bad retrieval. And changing the corpus mid-interview would leave the tracker's coverage verdict and the report's D-58 snapshot describing two different sets, which is exactly the disagreement D-58 exists to prevent. | Low. The refusal is a message, not an exception the user sees. |
| **D-62** | **Previewing a backup and restoring one are separate store calls** (`read_backup` vs `restore_generation`), and the restore keeps the version it replaced. | Looking at a backup must not be able to cost the user their live file, and a preview routed through the restore path would rotate the generations out from under the list the user is reading — the row they click would no longer be the version they previewed. Keeping the replaced version as generation 1 is what makes a one-click restore of a file the user cannot fully read on screen safe: the wrong choice is undoable. | Low. One extra parse per selection, on a dialog opened by hand. |
| **D-63** | **A corrupt active set is recovered at startup rather than skipped** (FR44), and the *offer* lives in the editor. | Falling through to the next candidate is how a user's notes become an empty editor: their set is on disk with five readable backups beside it and the app opens showing nothing — the "starting empty" FR44 names, and the failure most likely to be read as "the app lost my notes". The visible offer needs a window, and startup runs before there is one, so the two halves are split: recover-and-record before any UI exists, offer-and-choose once there is one. **A `SchemaTooNewError` is never recovered** — the backups are the newer format too, so restoring one would be this build overwriting data it cannot read. | Low. The recovery is recorded; the startup notice needs T9.6a's surface. |
| **D-66** | **The Qt test harness drains the event queue after every test**, not only at session end. | An unparented `QDialog` is owned by Python, so it dies the instant a test's last reference goes — immediately, not deferred — and anything still queued for it is dispatched against freed memory by whichever test next pumps the loop. Here that was the overlay clock test's two-second `processEvents` spin, so the crash landed twenty files away from its cause. The session-scoped teardown cannot help: by then the objects are long gone. Fifth destroy-order defect in this harness after D-53, D-54 and PR #27's two, and the second to appear as a segfault with every test reported passing. | Low. A `processEvents` per test, skipped entirely when Qt is not loaded. |
| **D-68** | **`CaptureStream` holds a silent render stream open on a loopback endpoint for as long as it captures** (the "keep-alive"), and finds that endpoint by matching **host API as well as name**. | Measured on the target machine in M1, and it contradicts what the capture code was written to assume: a WASAPI loopback endpoint with nothing playing delivers **no callbacks at all** — 0 frames in 6 s — rather than frames of silence. With a silent render stream open on the same endpoint, the same 6 s yields **298 of 300**. This is correctness, not telemetry: `EnergyVad` closes an utterance after 700 ms of silence and learns silence *from frames*, so with none arriving an interviewer's question stays open until `max_span_s` force-cuts it 10 s later, and every downstream gap measured from `t_end` moves with it. The host-API half is not defensive — the same physical endpoint is listed under MME `[5]`, DirectSound `[16]` and WASAPI `[24]` with an identical name, MME sorts first and truncates names to 31 characters, so a name-only search returns the wrong host API's entry. The probe that *found* this bug had itself opened the MME entry, and only worked by luck. | Moderate, and paid in two places. The app holds an output stream open for the whole session, so Windows shows it as playing audio and the endpoint cannot idle down — visible to the user in the volume mixer, and worth a line in FR63's disclosure if it reads as surprising. A failed keep-alive is **recorded, not raised** (`keep_alive_error`): capture without it still transcribes with degraded boundaries, whereas refusing to open the device costs the interview. `keep_alive_active` is exposed so a change that stops wiring it fails a test rather than quietly degrading an interview — the same answer `wired_purge_hooks()` gives. |
| **D-67** | **The FR72 mark trails the headline in the editor's note list**, rather than leading it. | Qt's incremental search matches `Qt.DisplayRole` from the start of the string, so a leading glyph means a keyboard user typing a headline's first letters no longer reaches that note — navigation traded for a decoration. Leading would read better as a column of shapes, and overriding `keyboardSearch` would buy that back, but reimplementing Qt's multi-key timeout, cycling and wrap-around semantics is real complexity for a placement. The association still forms: the glyph is on every row of the user's own notes. | Low. A format string. |
| **D-64** | **The import's strategy detection runs until the user overrides it**, not once at load. | FR2 makes `.txt` auto-detected, and the paste path has no filename to detect from — so passing the combo box's value unconditionally would chunk pasted `Q:`/`A:` notes as Markdown headings and produce nothing, with the box showing a strategy the user never chose. Detection stopping the moment they *do* choose is the other half: a control FR2 requires would be worthless if it were overridden on the next press. | Low. One boolean. |
| **D-65** | **The import verifies the notes it is *keeping* before it removes the ones it replaces.** | `add_source` verifies the incoming notes and `NotesStore.save` verifies the whole set, which leaves a gap the modeless import dialog opens: a non-verbatim bullet typed into the editor while the dialog is up passes the first check and raises out of the second — inside a Qt slot, with the set already replaced in memory and unchanged on disk. Checking the survivors first preserves the property `add_source` was built around: nothing is destroyed until everything validates. | Low. One pass over notes of the other four kinds. |
| **D-45** | **Design §9b's text-scaling formula governs over its own anchor table**, and the table's middle row was corrected to match (bullets at h=220 are 14px, not 15px). | The two disagreed. Only the min and max rows are independent values; the default row is derived, and it was derived wrong. Asserting the table instead would test transcription rather than behaviour, and would have baked a discontinuity into a function §9b calls linear. | Low. 14px still clears §9b's own 13px floor. |
| **D-46** | **FR27's lock withholds dragging and the two resize edges that move the panel's origin (left, top); the right and bottom edges stay live.** | FR27 is about the panel wandering off where the user put it. A right-edge drag changes the size and moves nothing, so blocking it would cost a control for no requirement — and blocking nothing would let a left-edge drag walk a "locked" panel across the screen. | Low. |
| **D-47** | **Overlay resize clamps the size first and then recomputes the origin from the clamped size.** | The obvious version moves `x` by the raw delta and clamps the width separately, so once the width hits its minimum the panel keeps sliding sideways — a user dragging the left edge past the limit walks it off the screen, which is the exact failure FR55 exists to recover from. | Low, but it was a reachable way to lose the panel. |
| **D-48** | **A drag takes a position-only path that skips the restyle; only a resize restyles.** | `_apply_halo` builds a fresh `QGraphicsDropShadowEffect` on four labels per call. On the full path that runs at pointer rate, on the one surface NFR3 measures frame time against, with a video call already on the GPU. | Low. |
| **D-49** | **FR36's viewer is a new module, `ui/diagnostics_view.py`, not part of `indicators.py`.** | Design §1 lists `indicators.py` for the always-visible strip and `ring.py` for the buffer, and FR36's "viewable in-app" is neither. A scrolling event table docked to the overlay would defeat the glanceability the overlay exists for. Same class of deviation as `main_window.py`, recorded rather than absorbed. | Low. |
| **D-50** | **Elision is truncate-and-append only**, so a rendered bullet is always a prefix of the stored note plus fixed product copy. | FR23 permits ellipsis and FR11 forbids anything the user did not write; the two only coexist if the elision cannot substitute, reorder or rephrase. A middle-elide would look like a cosmetic improvement and would quietly end the guarantee, so the property is asserted rather than described. | Low. |
| **D-44** | **The tracker's coverage verdict is stored with the transcript.** | It is session state and dies with the session, while FR78a makes it the only valid basis for an absence finding. A report regenerated later would otherwise derive coverage from a reset tracker and report every point as uncovered. | Low, but it makes D-U8's regeneration promise real rather than nominal. |
| **D-42** | **The FR37 switch fans out to every cloud consumer, and refuses to flip with none registered.** | `attach_matching` took a single target because the pipeline was the only API consumer when the switch was written. A switch that reports success having switched nothing is D-23 exactly. | Low, but it was live in M11. |
| **D-43** | **The session record is fed before the stream router, not after.** | The router splits by purpose, so recording downstream captures half the conversation while the report claims to cover the meeting. | Low. |
| **D-39** | **`ReportGenerator` requires a `ReportConsent`; there is no default.** | Generation is when the interviewer's words leave the device, so it is the only place FR85 can be enforced against the thing actually happening. An optional field would let the feature ship with consent unwired and nothing failing — D-23's exact shape. | Low. |
| **D-40** | **No cipher fallback outside Windows: `default_cipher()` raises.** | Storing an interview transcript under weaker protection than FR82 promises would be a false privacy statement about a third party's words. | Low. |
| **D-41** | **Record truncation stops recording rather than dropping the oldest.** | Dropping oldest silently loses the opening while the report claims to cover the whole meeting. Both lose data; only stopping is visible, and FR75 makes the report say so. | Low. |
| **D-37** | **FR70 is enforced by rejection in code, coercion on load.** | On the load path, rejection turns one stray flag on one chunk into `NoteSetCorruptError` for the whole set. Dropping the flag satisfies FR70 exactly; refusing the file satisfies it at the cost of every other note in it. | Low, but it was a new data-access-loss path. |
| **D-38** | **The prefilter breaks below `min(tau_for(kind))` and looks notes up by dict.** | Per-kind thresholds removed the single-floor early exit, so the loop walked the whole corpus with a linear scan inside it — O(n²) against the 50 ms/200-note budget. | Low. |
| **D-35** | **`WIPED` is retained as an enum member with no edges in or out**, and a test asserts it stays unreachable. | The state still describes a real behaviour that may return. Deleting it loses the vocabulary; leaving it wired loses the guarantee. The test is against the transition table, not against a handful of driven paths — the point is that *no* edge exists, which sampling cannot show. | Low. |
| **D-36** | **The WIPED branches in `resume()` and `end_session()` were deleted, not disabled in place.** | Unreachable code cannot be tested, and untestable branches that "handle" impossible cases rot into wrong answers. D-22's reasoning — a machine event must never undo a deliberate stop — survives in this record and is still *enforced*, because `PANIC` is absent from `AUTO_RESUME_CAUSES`. | Low. |
| **D-33** | **Schema v1 note sets migrate to v2 mapped to `PREP`, preserving IDs.** | `PREP` is the only kind that preserves existing behaviour, since v1 notes were trackable and FR70 still permits tracking on `PREP`. IDs must survive because the embedding cache is keyed on them (FR34). Without the migration, every existing user's notes load as corrupt and recovery finds every backup equally unreadable. | **Was a live data-loss path** in the first draft of M10 — found in review. |
| **D-34** | **Report findings carry either presence or absence evidence**, and absence is adjudicated by the tracker's mechanism (FR78a), not re-derived. | Two of four rubric dimensions produce their best findings by absence, which cannot cite an utterance. And a report that re-derives coverage will eventually disagree with the checklist the user watched live, with no principled way to know which to believe. | Low. |
| **D-29** | **`SourceKind` is immutable on a chunk**; reclassifying means delete and re-import. | A chunk's kind drives its threshold, its tracker eligibility and its enum quota. Mutating it in place would silently move a chunk between those regimes with a stale embedding still in the index. | Low. |
| **D-30** | **Stage-2 candidates are capped per kind (2), not globally.** | Otherwise a long job description dominates the enum by sheer chunk count and crowds out the prep notes the product exists to surface. | Low. |
| **D-31** | **Every report finding must cite the utterance it rests on**, or it is rejected before display. | The overlay cannot fabricate because it cannot generate. The report must generate, so the equivalent protection is anchoring. Without it the report is a model's impression of an interview it did not attend, told to someone who will believe it about themselves. | Medium — it is the whole trustworthiness argument for M11. |
| **D-32** | **Panic clear destroys the in-progress session only; previously saved sessions survive.** | Panic clear is single-action and unconfirmed (FR60). Destroying earlier interviews on one keypress is an irreversible surprise. FR83's delete-all is the deliberate route and FR87 signposts it there. | **Open — OQ-7.** Argued both ways in `07-…` §3. |
| **D-28** | **Frames in flight during a cloud→local fallback are dropped, not replayed.** | Replaying the tail double-transcribes the overlap, so the assembler builds two utterances from one span and matching fires twice on the same question. A missing half-second beats a duplicated question. | Low. |
| **D-12** | **`keyring` (Windows Credential Manager backend) for API keys**; no custom credential code. | FR19. | Low. |
| **D-17** | **PRISM is the design system for every UI surface.** It supersedes the tokens design §9b previously invented for the overlay. | A real design system beats per-component tokens, and this is the user's own brand system. | Low now; high once the overlay is built. |
| **D-18** | **The overlay is exempt from PRISM §6's "labels sit outside the card" rule.** Every other surface obeys it. | The overlay floats over a video call and has no canvas; "outside the card" would mean drawing onto the call. | Low. |
| **D-19** | **Headline in Plex Mono, bullets in Plex Sans.** | PRISM assigns mono to display and sans to body, and the split serves legibility: monospace scans more slowly, and the bullets are what gets read mid-sentence. | Low. |

## User product decisions — 2026-08-10 context sources & report

| ID | Decision | Consequence accepted |
|---|---|---|
| **D-U8** | The raw transcript is **persisted**, not dropped after report generation. | FR16 rewritten. Encryption at rest, session list, per-session delete and a retention default become requirements, not niceties. |
| **D-U9** | The report analyzes the **full meeting, both sides**, including the interviewer's reactions. | The saved artifact characterizes a specific named person. FR63's disclosure becomes insufficient; FR85 requires re-acknowledgement. |
| **D-U11** | **The panic control is on hold and now only pauses.** No wipe, no purge, no `WIPED` state. | The product loses its only in-session emergency data destruction, arriving alongside D-U8 which made transcripts persist — so nothing is destroyable mid-interview any more. FR64 superseded by FR64a; FR86 moot; OQ-7 closed unresolved. D-U5's "wipe, stop capture, stay resumable" is withdrawn. |
| **D-U10** | **All four rubric dimensions** — prep coverage, JD fit, resume utilisation, interview craft. | M11 hard-depends on M10; a missing source must be declared in the report, not silently skipped (FR77). |

## User product decision — 2026-09-09 local-first startup

| ID | Decision | Consequence accepted |
|---|---|---|
| **D-U13** | **First run requires network access, to choose and download models.** The user picks their STT model at first run, sees what they are choosing between, and the app downloads it. Every run after setup is offline. | Resolves **OQ-11**. Four things this settles, and one it creates. (1) **The catalog ships in the package**, so the picker and its explanations render with no network and only the weights need connectivity. Handy compiles its catalog in for exactly this reason (`catalog/mod.rs:1-7`). (2) **The user chooses the STT model, and never the embedder.** This asymmetry is forced by our own design: `EmbeddingIndex` keys its vector cache on `embed_model_id` and `embed_model_version` (`notes/index.py`), so a changed embedder silently invalidates every note's vectors and forces a full re-embed. The embedder is a fixed, silent dependency; only Whisper is a choice. Handy has no equivalent constraint, so do not copy its "all models are user-choosable" shape wholesale. (3) **Small and mandatory is bundled; large and chosen is downloaded.** Handy bundles its 1.8 MB Silero VAD as a build resource and fetches all 69 STT models at runtime. The VAD we adopt goes in the installer. (4) **Download hardening is a solved problem — copy it:** per-file pinned SHA-256, resumable `Range` requests, a stall timeout so it cannot hang, a cancel that keeps the partial for resume, and an upstream revision pinned to a commit. Improve on one point: Handy's later-launch check is a bare `path.exists()` with no re-verification, which for us would surface as an unexplained transcription failure mid-interview. **What it creates:** an interview can no longer be the first run. Setup is now explicitly a before-the-interview step, so preflight needs a `model_present` BLOCK check beside `notes_loaded` (`session/preflight.py`), and the first-run wizard has to say plainly that setup happens ahead of time. Note this does not unblock **AS-9**: this container's proxy blocks `huggingface.co` and `blob.handy.computer` alike, so model-loading stays verifiable only on the Windows target machine. |
| **D-U12** | **The program runs locally at startup. Cloud API keys are optional.** No account and no API key is ever required for the core loop: capture, transcribe, match, track. A key, when present, enables cloud STT (FR18–21) and the stage-2 LLM selector (FR10); its absence is a supported configuration, not an error. **Scope correction, 2026-09-09:** an earlier draft of this row said "and no network reachable", which overstated it. **First run does require network, to choose and download models** — see D-U13. What must work offline is every run *after* setup, which is the run that matters, because it is the one happening during an interview. | **This is the "no-API-key policy" T9.6a and T9.3 were blocked on, and it resolves it.** Most of the mechanism already exists and needs no new code: `config.stt_backend` already defaults to `LOCAL` (FR18), `Preflight.applicable()` already drops `cloud_only` checks when the backend is local, `CredentialStore.get()` already returns `None` rather than raising, and `MatchingPipeline` already accepts `selector=None` and degrades to the stage-1 embedding prefilter through `_local_only`. **Three things do follow.** (1) `Application` requires `client: MessagesClient` today with no default — it has to become optional, and `Stage2Selector` conditional on a key being present. (2) D-U3's degraded path stops being a failure mode and becomes the **default experience** for a keyless user, which front-runs part of OQ-1: stage-1-only now has to be good enough to ship on its own, not merely good enough to fall back to. (3) The report (M11) has no local generation path at all — `ReportGenerator` takes the client unconditionally — so a keyless user has no report. Raised as **OQ-10**. Separately, "runs locally" is not yet true even with this decision: no concrete `Embedder` implementation exists, and both the Whisper model and the embedding model are first-run downloads. That is **OQ-11**, and it is the real remaining content of T9.6a. |

## Assumptions — unverified, and what breaks if wrong

| ID | Assumption | If wrong |
|---|---|---|
| **AS-1** | `faster-whisper` `base.en` int8, **CPU-only on the 2025 laptop** (D-U6), achieves **p95 < 900 ms of inference tail** after utterance finalisation — the STT slice of the NFR1 budget in design §9a, not the whole 3 s. | Local default is not viable; cloud STT (already in v1 per D-U1) becomes the default and §10a's recommendation inverts. **This is a gate, not a hope — and it has not fired yet.** T2.4, the harness that measures it, has not been run. See `06-progress.md`'s Blocked register: it needs the D-U6 laptop specifically, CPU-only, not a container figure. |
| **AS-2** | `pyaudiowpatch` can open WASAPI loopback and a mic input device concurrently and stably for 60 minutes. | D-U2's dual-stream requirement needs a different capture library. **Partly measured on 2026-08-16** — the library and the concurrency hold: both endpoints opened together and ran at **100% of expected frames for 60 s**, worst drift 40 ms against the 50 ms budget, no drops, no callback errors. So the *library* question is answered and the escalation clause is not triggered. **The 60-minute half is still open**, and it is now the only half. Note the 60 s run needed continuous playback to mean anything, because of the defect below — with **D-68**'s keep-alive the hour can run with natural pauses, which is the condition the product actually meets. |
| **AS-3** | `all-MiniLM-L6-v2` cosine similarity separates relevant from irrelevant notes well enough that τ_floor suppresses most small talk. | Stage-2 call volume and cost rise sharply; may need a question-detection heuristic before stage 2. **Not yet measured.** T4.7 is the gate; per `06-progress.md`'s Blocked register it needs the user's hand-labelled mock-interview transcripts plus an `ANTHROPIC_API_KEY`, neither of which is in the environment. |
| **AS-4** | The user authors or confirms bullet-shaped notes at import (D-5). | Most notes fall to the D-6 truncation path and the overlay is less glanceable than FR11 intends. Mitigated by the importer nudging toward bullets. |
| **AS-5** | Interviews are single-interviewer or the user tolerates degraded attribution on panels. | PRD §12 already flags this. No v1 mitigation. |
| **AS-6** | Sentence-level chunking of `.md` by `##`/`###` headers matches how the user actually writes notes. | Import produces poor chunks; mitigated because FR2 requires review-before-save. |
| **AS-8** | **The Deepgram and ElevenLabs wire protocols are as implemented** — message names (`CloseStream`, `start`/`end`), envelope shapes, and timestamp semantics (Deepgram start+duration, ElevenLabs start/end). Written from documentation, **not verified against a live endpoint**. | The backends connect and transcribe nothing, or finals never flush. Contained: both are off the default path (FR18) and FR21 falls back to local. **Verify with a real key alongside T9.5.** |
| **AS-9** | **The `FasterWhisperTranscriber` adapter has never loaded a model.** The dev container's network policy denies `huggingface.co` (the proxy answers 403 to CONNECT), so no model file can be downloaded here. The `WhisperModel(...)` constructor arguments and the `Segment.text` / `Segment.avg_logprob` field names are written from the `faster-whisper` documentation, **not verified against the library**. | Local STT — the *default* backend (FR18) — raises on first use and the product transcribes nothing out of the box. Contained: the adapter is ~20 lines behind a `Transcriber` Protocol, and everything around it (VAD, FR47 finalisation, capture-clock timestamps, backpressure, threading) is tested against a double. **Verify on the Windows machine alongside T2.4**, which loads a real model anyway. |
| **AS-7** | Anthropic API p95 latency for a ~500-token request stays within the **800 ms stage-2 allocation in design §9a's latency budget**. *(Previously cited §7, which is the health model and contains no budget, and said "a few hundred ms", which is not the number the design commits to.)* | The 2–3 s overall target is missed on the matching path; D-11's supersession keeps it from compounding. **Not yet measured** — same T4.7 gate as AS-3, blocked on the same missing fixtures and API key. |

## Open questions — do not block v1, revisit at the stated milestone

| ID | Question | Revisit |
|---|---|---|
| **OQ-1** | Does stage 2 (LLM) measurably beat stage-1-only matching on real transcripts? Review A21. | **M4 gate.** If the delta is small, D-U3's degraded path becomes the normal path and the hard internet dependency is reconsidered. |
| **OQ-2** | Is τ_visible (snippet auto-clear, 25 s default) right for real interview pacing? | M5, then rehearsal in G2. |
| **OQ-3** | Should the progress tracker require headphones outright rather than warning? | M7, after D-8's echo detection is measured. |
| **OQ-4** | Local matching model (Ollama) as a fully-offline alternative to stage 2. PRD §10b. | Post-v1, informed by OQ-1. |
| **OQ-10** | What does the post-interview report do for a keyless user? D-U12 makes a key optional, but `ReportGenerator` requires one and no local generation path exists. Options: hide the report until a key is added, keep it as the one explicitly cloud-gated feature with a stated reason, or generate a reduced evidence-only report locally from the tracker's covered/missed sets without prose. | Before T9.6a wires the composition root — it decides whether `client` is optional-with-degradation or optional-with-a-disabled-feature. |
| ~~**OQ-11**~~ | **RESOLVED 2026-09-09 by D-U13** — first run requires network to choose and download models. Original question: how are the two local models acquired, given that the Whisper model and the embedding model are both first-run downloads and no concrete `Embedder` exists at all? | Answered. The remaining work is implementation, tracked under T9.6a. |
| ~~**OQ-6**~~ **RESOLVED — PRISM absorbed the fix.** `--ink-400 #9C94A8` is now the dark-mode secondary text token in PRISM itself. Original finding: | ~~PRISM's dark-mode secondary text token fails WCAG AA, and dark is the default mode.** `--ink-600` at 80% over `--plum-900` composites to `#504B59` — **1.96:1**, against the 4.5:1 required for 15px body copy; solid over `--plum-950` it is 2.51:1. PRISM §1 explicitly asks for this to be re-verified, and it does not pass. Proposal: `--ink-400 #9C94A8` (5.69:1 / 6.06:1). This app uses `--ink-400` now rather than waiting — an inaccessible default is not shippable while a token question is open — but PRISM itself should probably absorb the fix. | **Needs the user**, as a change to their design system. Not blocking: the app already uses the accessible value. |
| ~~**OQ-5**~~ **RESOLVED — `--amber-500 #FFC93D` added to PRISM** as its fifth palette dot, semantic "warning / degraded". Original finding: | ~~PRISM has no warning token. Its four semantic dots are danger, info, success, highlight. Two states here are none of those: a degraded match, and data leaving the device. Both mean "proceed, but know this" — red overstates them and purple already means "new". Proposal: add `--amber-500: #FFC93D`, taken from the existing `--grad-3` stop so it stays in-family. Implemented in the mockup and in §9b pending approval; the fallback is `--purple-500` for both, at the cost that blue and purple are harder to separate at a glance than blue and amber, on the one surface where glanceability is the point. | **Needs the user.** Blocks nothing, but the mockup already assumes the proposal. |
